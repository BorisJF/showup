Add Frustration as a fourth way to respond (sw v29)

🧘 Acceptance → 👶 Frustration → 💣 Retaliation → 💼 Diplomacy.

Frustration sits second, so the four read as an escalation: resolved
inside, unresolved inside, destructive outside, constructive outside —
with Diplomacy last as the one to aim for.

👶 moves from Retaliation to Frustration; Retaliation takes 💣.

"Stay annoyed, but keep it to yourself." — phrased as a command to match
the other three rather than "You are annoyed, but don't share it with the
world.", so the four read as one set.

Card/label copy goes three → four in both languages, and the chips take
the fourth band accent (indigo / rose / mint / plum).

Also fixes two static strings that JavaScript overwrites on load but
which were wrong in the source: the ways eyebrow said "Three ways to
respond", and the theme toggle said "Papier Tigre" when Cutestom is now
the default and the button should read "Original".
