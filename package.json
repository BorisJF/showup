{
  "name": "showup",
  "version": "2.0.0",
  "description": "SHOW UP — daily morning check-in",
  "type": "module",
  "scripts": {
    "test":       "vitest run",
    "test:watch": "vitest",
    "test:unit":  "vitest run tests/unit",
    "smoke":      "bash tests/system/smoke.sh"
  },
  "dependencies": {
    "@netlify/neon": "latest"
  },
  "devDependencies": {
    "vitest": "^2.0.0"
  }
}
